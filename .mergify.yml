merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: vm5-protected-cleanup
    queue_conditions: true
    merge_conditions:
      - check-success = vm5-never-check
    batch_size: 1
    max_checks_retries: 1
    checks_timeout: null
    merge_method: merge
